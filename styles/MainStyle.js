import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  placesList: {
    paddingLeft: 10,
    paddingRight: 10,
    paddingBottom: 10,
    flex: 1
  }
});

export default styles;
